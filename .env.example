# Shared
NODE_ENV=development

# API
API_PORT=3001

# Web
# Base URL used by apps/web Axios client
NEXT_PUBLIC_API_URL=http://localhost:3001

# Comma-separated list of allowed frontend origins for credentialed CORS.
CORS_ORIGIN=http://localhost:3000

# Optional cookie domain for production subdomains (e.g. .example.com).
# Leave empty for localhost and most cross-site setups.
AUTH_COOKIE_DOMAIN=

JWT_ACCESS_SECRET=change_me_access_secret
JWT_REFRESH_SECRET=change_me_refresh_secret

# PostgreSQL
# Format: postgres://USER:PASSWORD@HOST:PORT/DB_NAME
# If password contains special characters (!, @, %, #, *), URL-encode it.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/task_management
